# Companies House Accounts Parser
A tool to convert Companies House annual accounts files from .html or .xml into .csv files.

# Usage
`./ch-accounts-parser [directory]`

creates an `output.csv` file in the working directory.
